# 1. Типы данных в CSS?

    -- String
    -- Number 
    -- BigInt
    -- Boolean
    -- Symbol
    -- Object
    -- Null
    -- Underfined

# 2. Разница между == и === ?

    -- == просто сравнивает значение ( 1 == "1" true)
    -- === сравнивает их типы ( 1 === "1" false)

# 3. Разница между FD и FE?

`Function Declaration` - это функция которое создан основном потоке документа,  можно вызывать его до объевление `Hosting` - Сплытие
    fucntion sum(a,b){
        return a + b
    }

`Function Expression` - это созданная функция присвается к переменну
    const expresFunc = fucntion (a,b){
        return a + b
    }

# 4 Разница между Null и Underfined?

`Null` - это явное задание отсутсвуещего значение
`Underfined` - неопределенные данные, которое еще не присвоенно

# 5. Что такое Hosting?

`Hosting` - это механизм поднятие функции, переменный глобольную или функциональную область видимости.

# 6. Операторы  &&(И) и ||(ИЛИ)

`&&` - он возвраещает первое ложное значение либо последнего значение
`||` - находить и возв-т первое истиное значение либо последное значение

# 7. Типы таймеров в JS?

`SetTimeout` - позв-т вызывать функцию через определенную секунду
`SetInterval` - позв-т вызывать функцию постоянно через определенную секунду
             -- нужно вызывать ClearInterval - для остоновление интервала

# 8. Что такое область видимости (Scope)?

`Scope` - это места откуда мы имеем доступ к переменным и функции
`Глобальный` - переменый и функция объя-я для общего пространство
`Локалбный` - п/ф объя-й внутри функции доступный только этой фунции и всем вложенным элементами 
`Блочная` - находиться внутри фигурных {} скобках (блока) if...else, for()

# 9. Разница между let, const, var?

`var` - разное область видимости, всплывает, вызывается до его объявление
`let` - область видимости блочное, всплытие до объяв вызывает ошибку.
`const` - область видимости блочное, всплытие до объяв вызывает ошибку. Невозможно изменить данные

# 10. Что такое HOF?

`HOF` - это функция которое возвр другой функцию или пренимает другой функцию качестве аргумента.

` const HOF = (param, callback)=>{ return callback(param)}`

# 11. Что такое DOM?

`DOM` - объектный модель документа который браузер создасть в памяти комп-а на основани HTML кода.
      -- интерфейс которые JS можно взаймодействовать HTML с страницей.

# 12. Что такое распространение события (Event Propagation)?

`Event Propagation` - это механизм которое отрабативает когда какиеөлибо событие происходит документе.
                    -- событие распро-я от объекта window до вызываещего элемента, последовательно затрагивает всех предков целового элемента

# 13. Как превратить любой тип данных в булевый? Перечислите ложные значение в JS?

`Boolean(10), !!('str')`
`Falsy values` 0, false, ' ', null, underfined, NaN

# 14. Методы строк в JS?

`length` - длина строкий
`CharAt(1)` - 
`toUpperCase()` - регистр строк заглавный
`toLowerCase()` - регистр строк нижный
`indexOf('s')` - поиск элемента / возвр index
`lastIndexOf('s')` - поиск элемента / возвр послед index вхождение
`slice(0,3)` - обрезка строк
`replace('s','d')` - для замена строк
`trim()` - удаляеть пробельные символи нач/пос

# 15. Методы массива в JS?

`length` - длина массива
`push('fin')` - доб элемента в конец
`pop()` - удаль элемент в конце
`unshift('fin')` - доб элемент в начало
`shift()` - удаль элемент в начало
`concat([])` - обед-е массивов
`splice(0,1)` - обрезка массива
`toString()` - прев в стринг
`join('')` - прев-е в строку с доб элемента между

# 16. Разница между e.preventDefault() и e.stopPropagation()?

`e.preventDefault()` - откл поведение элемента по умолчанию
                     -- исполь на ссылке при клике отмен-е перехода
                     -- иполь на form  при клике отмен-е отправку формы
                     
`e.stopPropagation()` - откл распространение событие
                     -- откл всплытие
                     -- откл погружение

# 17. Методы поиск элементов в DOM?

`getElementById` - по идентификатору
`getElementByTagName` - по тегу, вернет все теги
`getElementByName` - по атрибуту name 
`getElementByClassName` - по имени класса
`querySelector` - любой один селектор
`querySelectorAll` - любой коллекционный селектор

# 18. Что такое чистая функция?

`Чистая функция` - должна удволи-т два условия
                 -- не должен быть побочных эффектов ( запросы, вывод на экран, переменное, изменение входных данных)
                 -- каждый раз когда  оно вызывается возрв одинковый результат, когда вызывается с темже наборами аргументов

# 19. Разница между forEach и map? 

    Оба метода используется для перебирание массива 
`forEach` - перебирает массив и ничего не возвр-т
`map` - перебирает массив и возвр-т новый массив

# 20. Что такое this в JS?

`this` - это контекст вызова или ссылка назначение объекта котрое данный момент выполь-я.
        this - (Глобальный объект, обявление, объект событие)


# 21. Почему в JS функция называет объектами первого класса?

    Потомучто в JS любые типы данных ведут себя как объекты
        -- их можно присвоит в переменну
        -- функция можеть быт свойствам объекта
        -- также её можно передат в другою фунцию в виде аргумента

# 22. Как определить наличие свойств в объекте?

`hasownProperty` - console.log(obj.hasownProperty('prop1'))
`in` - console.log('prop1' in obj)
`index nation` - прямой обра-ие console.log(obj['prop1'])

# 23. Что такое замыкание?

`Замыкание` - внутри одной функции создав другой функцию то вложенная функция получить доступ к переменным внешный функии

# 24. Что такое IIFE?

`IIFE` - это анонимная самовыз-я функция

# 25. Что такое псевдомассив Arguments?

`Arguments` - это коллекция аргументов которое передоется в функции

           -- у него есть свойство length
           -- но другие методы массива ему не доступны
           -- с помощью его можно получить доступ к любому из аргументов переданное в функции

# 26. Разница между  host и native объектами?

`Host` - это объекты которые предостовляется средами браузера (window, location, history)
`Native` - это объекты которое явлется частами языка JS (str, obj, function, array)

# 27. Разница между e.target  и e.event.target?

`e.target` - это элемент на котором происходить событие
`e.event.target` - это элемент которому прекрелен прослушиватель событие

# 28. Разница между stopPropogation и stopimmediatePropogation?
     При наступление событие в баузее происхдит сплытие
`stopPropogation` - отменяет дальнейше всплытие, но на текущии элементе все оброботчики срабативает
`stopImmediatePropogation` - не только остоновит дальнейще всплытие но и остоновливает обоботку событие не текушим элементе

# 29. Разница между Load, DOMContentLoaded?
    Обе событие срабатывает при загузки веб страницы в браузера
`Load` -  это когда браузер загрузиль HTML и все зависимые ресурсы
`DOMContentLoaded` - отработает когда браузе поль-ю загрузиль HTML после чего построен DOM дерево, однако внешные ресурсы (стиль, скрипты, картинки) не загружены

# 30. Разница между Синхр / Асинхр function?

    Синхронная функция блокируещим а асинхр функция нет
`Синхр` - во время выполнение Js кода интерпретатор натыкается на синхр.функцию он блокиреут даль-е выполнение операций ожидание завершение синхр.функцию
`Асинхр`- не блокирует выполнение даль-е операций 
        ** запрос данных делает асинхронно

# 31. Что такое прогресивное рендеринг?

`Прогресивное рендеринг` - название техгологий которое используется для ускорение отресований веб-страниц
                        ** основная идея как можно ранше показать пользователю визуальный контент
                        ** осноная задача это уменчение времени загрузки

# 32. Почему сравнение двух объектов false?
    В JS объекты сравнивается по ссылке или по адресу помяти где хранится переменная
    Когда создается объекты даже внутренные структуры одинаковое у них получается разное

# 33. Что такое NAN?

`NAN` - означает вычислительную ошибку
    ** оно получается когда матиматическое значение срабатало не верно 
    ** NaN не равно не каким данным даже самому себя

# 34. Что такое ECMA Script? Отличие?

`ES6` - спецификация стандарта Ecma-26 нововедение правил и обнолений для языка скриптовый констр

# 35. Что такое объектная обертка (Wrapper Object)?

    Приметивы имеют свойтсва и методы, несмотря на то что они не явлются объектами.
Когда к примитивам принимается свойства и методы они временно преобразуется в объекты для вызова методы - это называется `Wrapper Object`

```javascript
    let name = "make";
        name.toUpperCase()
```

# 36. Как в JS создать Object?

    Есть три способа создать Object:
                                    1. Объектный литера: обычные объекты с фигурными скобками ({})
                                    2. Function Коонструктор: - это функция которые вызывается только через оператор new ()
                                    3. Метод object.creat: - создает новый object к указанным прото-м и свойствам

# 37. Для чего используется ключевое слова new?

`Ключевое слова new` - используется в функций конструктурах для создания нового объекта

    Делает 4 вещи:
                1. создает пустой объект
                2. прис-т к нему значение this
                3. функция наследует от прототипа
                4. возвращает значение this если не указоно нечего

# 38. Разница между Rest|Spread операторами?
`Spread` - передаем или разспространяем данные массива на другие данные
`Rest` - исполь-я в функциях в передачи аргумента и деструктуризация

# 39. Что такое деструктуризация?

`Деструктуризация` - новый способ получения значения объекта и массива

# 40. Что такое цикл событий (Event Loop)?

`Event Loop` - это однопоточный цикл, который контролирует стек вызыва  и проверяет есть ли какое-либо работа которую необходимо выполнить в очереди задачи.
            ** события / выполнение операция данных в JS
                    1. console.log()
                    2. fetch()
                    3. setTimeOut()

# 41. Для чего используется оператор !! ?

`!!` - оператор приводит значение справо отнего к логическому значению.
    console.log(!!null) / false

# 42.Как проверить, является ли значение массивом?

    Для этого следует испоьзовать метод Array.isArray
    console.log(Array.isArray(5)) / false

# 43. Для чего испоьзуется оператор % ?

`%` - вернет остаток от делений
    console.log( 5 % 2 ) / 1

# 44. Как работает boxing/unboxing в JS?

`Упаковка \ Распаковка` - это процесс преоб-я премитива в объект \ преоб-я объекта в премитива

# 45. Для чего используется цикл  for...of ? 

`for...of` - выполняет цикл обхода объекта, массива, строки  вызывая каждом щаге итерации значение свойств

```javascript

    let sum = [4, 5, 6];
    
    for(let v of sum){
        console.log(v)
    }

    // 4, 5, 6
```

# 46. Что такое шаблонные литералы (Template Leterals)?

`Template Leterals` - новый способ создание строк в JS (`...`)
                    ** не нужен использивание \n для перевода сторки
                    ** не нужен использивать конкетенацию строк для объединения текста с переменной, а будем использовать ${}

# 47. Что такое Set/Map WeapSet/WeakMap?

`Set` - это объект позволять хранит уникальное значение примитивы и ссылки на объект
    ** add - добавить
    ** delete - удалить
    ** has - проверить наличие
    ** size - размер
    ** clear - очистит

`Map` - это коллекция ключ/значение объект, отличие от обычного объекта позволяет использовать ключ любого типа
    ** set(k, v) - 
    ** get -
    ** has -
    ** delete - 
    ** clear -
    ** size - 

`WealMap` - отличие от Map ключи должен быть объекты и удаляться когда они становится недоступным 
`WealSet` - подобная коллекция, хранит только объекты и удаляться когда они становится недоступным 

# 48. Какие методы можеть быть имееть HTTP-запросы?

`get` - запрашивает представление ресурса
`post` - используется для отправки данные
`delete` - удаляет указанный ресурс
`path` - исполь-я для частичного изменение ресурса
`head` - похоже на get, но возвр тело ответа
`put` - отправка данные на изменение, обновить запись на ресурс

# 49. Что такое HTTP cookie?

`Cookie` - это фрагмент данных, которых сервер отправляет браузеру пользователя. Браузер сохранить этот фрагмент у себя и отправляеть на сервер с каждом последующим запросом
        ** можно запольнить определенные данные которые мы водим на том или ином сайте и при повторном посещений эти данные запрошиватся не будеть
        ** управление сеансом (логиныб корзины для поле)
        ** пользовательские (пользовательские предпочтения)
        ** трекинга (отлеживание поведения пользотелья)

# 50. Что такое мемоизация

`Мемоизация` - это прием создания функции способной запомнить ранее вычисленные результаты.
        ** Плюс - мы избегаем повторного выполнение функции с одинаковым аргументами
        ** Минус - мы вынуждены выделять дополнительную память для сохранения резултатов

# 51.  Разница оператором in и методом hasOwnProperty?

`in` - проверяет наличие своиств не только в самом объекте, но и его прототипах
`hasOwnProperty` - проверяет наличие своиств только в самом объекте

# 52. Разница между глубокой/поверхностной копирование объекта

`Поверхностной` - при копирование массива, объекта JS копирует данные только на один уровень в глубь, которое указывает н тоже место в памяти, что и оригинал
`Глубокой` - не будет имеет общих ссылок с исходной переменной. Можно использивать spread, JSON

# 53. Что такое цепочка вызовов функций?

`Цепочка вызова` - конструкция, при котором вызов одного метода следует сразу за вызавом другого

# 54. Что такое необъявлененная переменная?

`Необъявлененная` - это происходит, когда мы пытаемя получит доступ к любой переменной, которой не было создана заранее / Reference Error
`Неопределенный` - это происходит, когда пер-я было объявлена, но ей не было присвоено никакого значения / underfined

# 55. Как передаются параметры в function?

`По значению` - строка, числа, боулин, передается в функцию по значение, функция получает копию данного значение
`Передача по ссылке` - объекты и массивы передаются по ссылке, функция получает сам объект и массив а не копию

# 56. Разница между обычном и стрелочном function?

    Разница между обычном и стрелочном function:
            ** Синтаксис
            ** Конструкторы
                -- оф - легко может создавать конструкторы
                -- сф - не имеет собственное this, при создание конструкторы object вывод Error
            ** Object Arguments
                -- оф - внутри сущ-ть спец-й массив аргументс
                -- сф - внутри не сущ-ть спец-й массив аргументс
            ** return
                -- сф - при однострочным коде без return может возврөть значение

# 56. Разница между object.freeze()/const?

`const` - гарантирует, что создаанная переменная доступна толькр для чтения, но можно изменить значения свойств объекта
`object.freeze()` - заморозить значение свойств объекта будеть неизме-и

# 57. Что такое Api?

`Api` - набор готовых функции и своиств которым мы можем взаймодействоват программы с сервером

# 58. Виды событие JS?
    В JS есть следущие типы событие:
        1. Событие мыши:
            ** click - нажатие мышки
            ** dbclick - два нажатие мишки
            ** mousedown - удер нажатие мыши
            ** mouseup - опускание мыши
            ** mouseover - курсор над элементом
            ** mouseout - уход от элемента
            ** mousemove - движение над элементом
        2. Событие клавищи:
            ** keydown - клав нажата
            ** keyup - клав опускание
        3. Событие скролл:
            ** scrollY() - скролл верт
            ** scrollX() - скролл гори
        4. Событие загрузка страницы:
            ** DOMContendLoaded - до загрузки стили и скрипты
            ** Load - все ресурсы загружены
            ** Unload - покидание страницы

# 59. Как добавить обработчик события DOM-элемент?

    Назначить обработчик можно разными способами?
        ** Через HTML атрибуты - примое обрашение обработчика в HTML страницы
        ** Свойства DOM элемента - назначение оброботчик событие в DOM дереве
        ** подписка на addEventListner - использоват метод addEventListner

# 60. Как удалитьт обработчик события DOM-элемента?

    removeEventListner()

# 61. Сколько аргументов пренимает addEventListner?

```javascript
    addEventLisner(event, handler[options])
    
    const buttonThree = document.querySelector(".dev__click--three");
	buttonThree.addEventListener("click", function(e) {
		console.log('Click')
	});

    // event - имя события
    // handler - функция когда будеть сраб обр-к
    // options - не обяз-й, дополнителный параметр
        // once - будет вызыватся один раз
        // capture - задаёт фразу на который нужна вызывать обр-к событий (погружение/вспылите)
        // passive - опред-т следует вызывать preventDefault()

```